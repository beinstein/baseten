// Set the deployment target to 10.4 since the resulting library
// might even work with non-gc applications on 10.4. Garbage collection
// seems to require 10.5 SDK though.
GCC_ENABLE_OBJC_GC = supported
